apiVersion: v1
kind: Secret
metadata:
  name: roomease-secret
  namespace: roomease
  labels:
    app: roomease
type: Opaque
# To encode your MONGO_URI run:
#   echo -n "your_mongo_uri_here" | base64
# Then paste the result below.
data:
  MONGO_URI: REPLACE_WITH_BASE64_ENCODED_MONGO_URI
