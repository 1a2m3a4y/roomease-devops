apiVersion: v1
kind: Service
metadata:
  name: roomease-service
  namespace: roomease
  labels:
    app: roomease
spec:
  type: ClusterIP
  selector:
    app: roomease
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
